# ARCHITECTURE — <项目 / 需求名>

> 本文件只在需求影响结构时创建。
> 它描述**现有结构是什么**以及**本次方案会如何改变结构**。
> 不写需求规则，不写进度，不解释取舍理由。

## 0. 状态

| 字段 | 内容 |
|---|---|
| 状态 | 草稿 / 已确认 / 已完成 |
| 创建原因 | <例如：新增模块边界 / 数据流变化 / 配置契约变化> |
| 最后更新 | YYYY-MM-DD |

## 1. 现有结构快照

| 模块 / 文件 | 当前职责 | 与本需求关系 |
|---|---|---|
| `<path>` | <一句话> | <会复用 / 会修改 / 只读参考> |
| `<path>` | <一句话> | <会复用 / 会修改 / 只读参考> |

## 2. 当前调用链 / 数据流

```text
<入口>
  -> <现有模块 / 方法>
  -> <现有模块 / 方法>
  -> <输出 / 副作用>
```

## 3. 目标结构

```text
<入口>
  -> <保留的现有模块>
  -> <新增或调整的模块>
  -> <输出 / 副作用>
```

## 4. 模块与接口契约

| 模块 / 文件 | 新增 / 修改 | 职责 | 不应依赖 |
|---|---|---|---|
| `<path>` | 新增 / 修改 | <一句话> | <边界> |

### 4.1 <接口或数据结构名>

```ts
// 仅示意形状；按项目语言替换
interface ExampleContract {
  id: string;
}
```

约束：
- <输入 / 输出约束>
- <错误处理或兼容性约束>

## 5. 数据、配置、资源变化

| 类型 | 路径 / 字段 | 变化 | 兼容性 |
|---|---|---|---|
| 数据 | `<path-or-table>` | <新增 / 修改 / 不变> | <迁移 / 兼容 / 无影响> |
| 配置 | `<KEY>` | <新增 / 修改 / 不变> | <默认值> |
| 资源 | `<path>` | <新增 / 修改 / 不变> | <加载方式> |

## 6. 测试与观测点

- <需要覆盖的单元 / 集成 / 手工路径>
- <日志、指标、调试入口或可观察行为>

## 7. 已知结构风险

| 风险 | 影响 | 处理 |
|---|---|---|
| <例如：现有模块耦合过深> | <影响实现或回归风险> | <缓解方式或接受理由> |
