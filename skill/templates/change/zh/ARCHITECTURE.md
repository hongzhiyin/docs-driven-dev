# ARCHITECTURE - <需求名>

> 本文件只在需求影响结构时创建。它描述现有结构是什么，以及本次方案会如何改变结构。

## 0. 状态

| 字段 | 内容 |
|---|---|
| 状态 | 草稿 |
| 创建原因 | <新增模块边界 / 数据流变化 / 配置契约变化 / 迁移等> |
| 最后更新 | YYYY-MM-DD |

## 1. 现有结构快照

| 模块 / 文件 | 当前职责 | 与本需求关系 |
|---|---|---|
| `<path>` | <一句话> | <会复用 / 会修改 / 只读参考> |

## 2. 当前调用链 / 数据流

```text
<入口>
  -> <现有模块 / 方法>
  -> <输出 / 副作用>
```

## 3. 目标结构

```text
<入口>
  -> <保留的现有模块>
  -> <新增或调整的模块>
  -> <输出 / 副作用>
```

## 4. 模块与接口契约

| 模块 / 文件 | 新增 / 修改 | 职责 | 不应依赖 |
|---|---|---|---|
| `<path>` | 新增 / 修改 | <一句话> | <边界> |

## 5. 数据、配置、资源变化

| 类型 | 路径 / 字段 | 变化 | 兼容性 |
|---|---|---|---|
| 配置 | `<KEY>` | <新增 / 修改 / 不变> | <默认值> |

## 6. 测试与观测点

- <需要覆盖的单元 / 集成 / 手工路径>
- <日志、指标、调试入口或可观察行为>
